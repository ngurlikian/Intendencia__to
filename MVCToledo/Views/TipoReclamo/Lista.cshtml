@model List<CommonSolution.DTO.dtoTipoDeReclamo>

@{
    ViewBag.Title = "Lista";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Numero De Tipo De Reclamo
        </th>
        <th>
            Nombre
        </th>
        <th>
            Descripción
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.NroDeTipoDeReclamo)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Nombre)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Descripción)
    </td>
    <td>
        @Html.ActionLink("Editar", "Modificar", new { NroDeTipoDeReclamo = item.NroDeTipoDeReclamo }) |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Eliminar", "Eliminar", new { NroDeTipoDeReclamo = item.NroDeTipoDeReclamo })
    </td>
</tr>
}

</table>
